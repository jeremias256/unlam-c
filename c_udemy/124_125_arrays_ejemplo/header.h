#ifndef header.h
#define header.h
#include <math.h>
#include <stdio.h>
#include <stdlib.h>

void datos_arreglo(int vec[]);
void llenar_arreglo(int vec[]);
int sumar_elementos(int vec[]);
int multiplicar_elementos(int vec[]);
int sumar_elementos_divisibles(int vec[]);
void multiplicar_por_tres(vec);
#endif

