#include "header.h"

void ordernar_por_borbujeo(int vec, int ce)
{
    int i=1,j;
    int hubo_intercambios = 1;

    while(hubo_intercambios && i < ce)
    {
        hubo_intercambios = 0;
        for(j=0;j<ce-1;j++)
        {
            if(v[j] > v[j+1])
            {
                intercambiar
            }
        }
    }

}
